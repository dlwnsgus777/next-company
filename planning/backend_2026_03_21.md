# 백엔드 작업 진행 현황

> 작성일: 2026-03-21
> 기준 계획: .omc/plans/back_phase_b_to_e.md

---

## 패키지 구조 원칙

- `domain/` — Entity, Repository (query/command 어디에도 속하지 않음)
- `query/` — 읽기 서비스 + 응답 DTO + GET 컨트롤러
- `command/` — 쓰기 서비스 + 요청 DTO + POST/PATCH/PUT/DELETE 컨트롤러

---

## 완료된 작업

### Phase A — 기반 구조 (이전 작업)

| 파일 | 내용 |
|------|------|
| `CompanyBackendApplication.kt` | `@EnableJpaAuditing` |
| `config/SecurityConfig.kt` | `/api/**` permitAll |
| `config/CorsConfig.kt` | localhost:3000 허용 |
| `config/entity/BaseEntity.kt` | createdAt, updatedAt |
| `common/ApiResponse.kt` | 공통 응답 래퍼 |
| `common/exception/GlobalExceptionHandler.kt` | 예외 핸들러 |

### 기반 작업 — 도메인 객체 및 DTO (2026-03-21)

#### 변경 사항
- `ApplicationStatus` → `JobChangeStatus` 로 이름 변경 (사용자 요청)
- 필드명 `applicationStatus` → `jobChangeStatus` 로 변경

#### 생성된 파일

**도메인 (TDD 적용)**

| 파일 | 테스트 | 결과 |
|------|--------|------|
| `model/company/domain/JobChangeStatus.kt` | — | 이동/이름 변경 |
| `model/company/domain/Company.kt` | `CompanyTest.kt` (4개) | PASSED |
| `model/company/domain/CompanyRepository.kt` | — | 작성 완료 |
| `model/kanban/domain/KanbanColumnConfig.kt` | `KanbanColumnConfigTest.kt` (2개) | PASSED |
| `model/kanban/domain/KanbanColumnConfigRepository.kt` | — | 작성 완료 |

**DTO (TDD 제외)**

| 파일 | 내용 |
|------|------|
| `model/company/query/dto/CompanyResponse.kt` | id, name, jobChangeStatus, memo, createdAt |
| `model/company/command/dto/CreateCompanyRequest.kt` | `@NotBlank name`, jobChangeStatus, memo |
| `model/company/command/dto/UpdateCompanyRequest.kt` | 부분 수정용 (nullable 필드) |
| `model/kanban/command/dto/KanbanColumnDto.kt` | id, label, statuses, accentColor, order |
| `model/kanban/command/dto/SaveKanbanColumnConfigRequest.kt` | `@NotEmpty columns` |
| `model/kanban/query/dto/KanbanColumnConfigResponse.kt` | columns: List<KanbanColumnDto> |

#### 테스트 결과
```
CompanyTest                  4/4 PASSED
KanbanColumnConfigTest       2/2 PASSED
CompanyBackendApplicationTests  1/1 PASSED
BUILD SUCCESSFUL
```

---

## 남은 작업

### 기능 1 — 회사 목록 조회 `GET /companies`
- [ ] `CompanyQueryService` TDD
- [ ] `CompanyQueryController` TDD

### 기능 2 — 회사 생성 `POST /companies`
- [ ] `CompanyCommandService.create()` TDD
- [ ] `CompanyCommandController` POST TDD

### 기능 3 — 회사 수정 `PATCH /companies/{id}`
- [ ] `CompanyCommandService.update()` TDD
- [ ] `CompanyCommandController` PATCH TDD

### 기능 4 — 회사 상태 변경 `PATCH /companies/{id}/status`
- [ ] `CompanyCommandService.updateStatus()` TDD
- [ ] `CompanyCommandController` PATCH/status TDD

### 기능 5 — 회사 삭제 `DELETE /companies/{id}`
- [ ] `CompanyCommandService.delete()` TDD
- [ ] `CompanyCommandController` DELETE TDD

### 기능 6 — 칸반 컬럼 조회 `GET /kanban-columns`
- [ ] `KanbanColumnConfigQueryService` TDD
- [ ] `KanbanColumnConfigQueryController` TDD

### 기능 7 — 칸반 컬럼 저장 `PUT /kanban-columns`
- [ ] `KanbanColumnConfigCommandService` TDD
- [ ] `KanbanColumnConfigCommandController` TDD

### DB 스키마
- [ ] `src/main/resources/schema.sql` 작성

---

## 현재 파일 트리 (`src/main/kotlin`)

```
com.company
├── CompanyBackendApplication.kt
├── common/
│   ├── ApiResponse.kt
│   └── exception/GlobalExceptionHandler.kt
├── config/
│   ├── CorsConfig.kt
│   ├── SecurityConfig.kt
│   └── entity/BaseEntity.kt
└── model/
    ├── company/
    │   ├── domain/
    │   │   ├── Company.kt
    │   │   ├── CompanyRepository.kt
    │   │   └── JobChangeStatus.kt
    │   ├── query/dto/
    │   │   └── CompanyResponse.kt
    │   └── command/dto/
    │       ├── CreateCompanyRequest.kt
    │       └── UpdateCompanyRequest.kt
    └── kanban/
        ├── domain/
        │   ├── KanbanColumnConfig.kt
        │   └── KanbanColumnConfigRepository.kt
        ├── command/dto/
        │   ├── KanbanColumnDto.kt
        │   └── SaveKanbanColumnConfigRequest.kt
        └── query/dto/
            └── KanbanColumnConfigResponse.kt
```
