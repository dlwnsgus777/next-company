# 프론트엔드 개발 체크리스트

> 작성일: 2026-03-01
> 기준 기획: planning_2026_03_01.md (칸반 보드 컬럼 커스터마이징)

---

## Phase A — 타입 & 스토어

- [x] `KanbanColumnConfig` 인터페이스 추가 (`src/types/index.ts`)
- [x] `DEFAULT_KANBAN_COLUMNS` 상수 추가 (`src/types/index.ts`)
- [x] `useAppStore`에 `kanbanColumns` 상태 추가
- [x] `useAppStore`에 `setKanbanColumns` 액션 추가
- [x] `ModalState`에 `kanbanSetting: { open: boolean }` 추가
- [x] `openKanbanSetting` / `closeModal('kanbanSetting')` 액션 추가
- [x] `persist` 대상에 `kanbanColumns` 추가

---

## Phase B — KanbanBoard 리팩토링

- [x] `KanbanBoard.tsx` 하드코딩 `COLUMNS` 제거
- [x] store의 `kanbanColumns`를 구독해 컬럼 렌더링하도록 변경

---

## Phase C — KanbanColumnSettingModal

- [x] `src/components/modal/KanbanColumnSettingModal.tsx` 생성
  - [x] 컬럼 목록 (이름 Input + 색상 선택 + 삭제 버튼)
  - [x] 드래그로 컬럼 순서 변경
  - [x] `[+ 컬럼 추가]` 버튼
  - [x] ApplicationStatus 배치 섹션 (상태마다 Select → 컬럼 선택)
  - [x] 미배치 상태 경고 표시
  - [x] 저장 비활성화 조건 (미배치 존재 OR 이름 빈 컬럼)
  - [x] 색상 프리셋 인라인 선택 UI
  - [x] 저장 / 취소 버튼
- [x] `page.tsx`에 `<KanbanColumnSettingModal />` 추가

---

## Phase D — 진입점

- [x] `Header.tsx`에 `[칸반 설정]` 버튼 추가 (칸반 뷰일 때만 노출)

---

## 작업 순서

```
Phase A → Phase B → Phase C → Phase D
```
