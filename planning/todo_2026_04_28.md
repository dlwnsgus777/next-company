# Front ↔ Back API 연동 TODO

> 작성일: 2026-04-28  
> 코드 확인 기준: 2026-04-28

---

## 현재 구현 상태 요약

- [x] 백엔드 Company 기본 CRUD API 구현
  - [x] `GET /companies`
  - [x] `POST /companies`
  - [x] `PATCH /companies/{id}`
  - [x] `PATCH /companies/{id}/status`
  - [x] `DELETE /companies/{id}`
- [x] 백엔드 KanbanColumnConfig API 구현
  - [x] `GET /kanban-columns`
  - [x] `PUT /kanban-columns`
- [x] 백엔드 `Company.id`는 `Long` 기준으로 구현
- [x] 백엔드 지원 상태 필드명은 `jobChangeStatus` 기준으로 구현
- [x] 백엔드 `company`, `kanban_column_config` schema.sql 작성
- [ ] 프론트 API 클라이언트 미구현
- [ ] 프론트는 아직 `MOCK_COMPANIES`, `MOCK_CRITERIA`, Zustand `persist` 기반 로컬 상태 사용 중

---

## 0. 남은 결정 사항

- [x] `Company.id`는 백엔드 기준 `Long`/프론트 `number`로 맞춘다
- [x] 지원 상태 필드명은 백엔드 기준 `jobChangeStatus`로 맞춘다
- [ ] 백엔드에 없는 Company 필드를 어떻게 처리할지 결정
  - [ ] `scores`
  - [ ] `targetStatus`
  - [ ] `jobPostingUrl`
  - [ ] `recruitmentDeadline`
- [ ] `Criteria` 기능을 백엔드 API로 확장할지, 프론트 로컬 상태로 유지할지 결정

> 현재 백엔드 `Company`는 `id`, `name`, `jobChangeStatus`, `memo`, `createdAt`, `updatedAt` 중심으로 구현되어 있다.

---

## 1. 프론트 환경 설정

- [ ] `front/.env.local` 생성

```env
NEXT_PUBLIC_API_URL=http://localhost:8080
```

- [ ] 프론트 실행 시 `NEXT_PUBLIC_API_URL`이 정상적으로 읽히는지 확인

> 백엔드 컨트롤러 경로가 `/companies`, `/kanban-columns`로 잡혀 있으므로 현재 코드 기준 base URL은 `/api` 없이 `http://localhost:8080`이 자연스럽다.

---

## 2. API 클라이언트 생성

- [ ] `front/src/lib/api.ts` 생성
- [ ] `NEXT_PUBLIC_API_URL` 기반 fetch wrapper 작성
- [ ] 백엔드 공통 응답 형태 `ApiResponse<T>` 타입 정의
- [ ] Spring Page 응답 타입 정의
- [ ] 공통 HTTP 메서드 함수 작성
  - [ ] `GET`
  - [ ] `POST`
  - [ ] `PATCH`
  - [ ] `PUT`
  - [ ] `DELETE`
- [ ] 4xx, 5xx 응답 에러 핸들링 추가
- [ ] 응답 JSON 파싱 및 빈 응답 처리 방식 정리

---

## 3. 프론트 타입 정리

- [ ] `Company.id` 타입을 `string`에서 `number`로 변경
- [ ] `Company.applicationStatus`를 `jobChangeStatus`로 변경
- [ ] `ApplicationStatus` 타입명을 유지할지 `JobChangeStatus`로 변경할지 결정
- [ ] 백엔드 `CompanyResponse` 타입 추가
- [ ] 백엔드 응답을 프론트 화면 모델로 변환하는 mapper 작성
- [ ] 백엔드에 없는 필드의 기본값 또는 로컬 유지 전략 반영
  - [ ] `scores`
  - [ ] `targetStatus`
  - [ ] `jobPostingUrl`
  - [ ] `recruitmentDeadline`

---

## 4. Company API 프론트 연동

### 4-1. 회사 목록 조회

- [ ] `GET /companies` 호출 함수 작성
- [ ] 페이지 마운트 시 회사 목록 fetch
- [ ] `useAppStore` 초기 `companies` 상태를 API 응답 기반으로 변경
- [ ] Zustand `persist` 대상에서 `companies` 제거
- [ ] 백엔드 Page 응답의 `content`를 화면 목록으로 매핑

### 4-2. 회사 생성

- [ ] `POST /companies` 호출 함수 작성
- [ ] 회사 생성 요청 DTO 타입 정의
  - [ ] `name`
  - [ ] `jobChangeStatus`
  - [ ] `memo`
- [ ] `addCompany` 액션을 API 호출 기반으로 변경
- [ ] 생성 성공 후 회사 목록 재조회 또는 응답값으로 상태 동기화

### 4-3. 회사 수정

- [ ] `PATCH /companies/{id}` 호출 함수 작성
- [ ] 회사 수정 요청 DTO 타입 정의
  - [ ] `name`
  - [ ] `jobChangeStatus`
  - [ ] `memo`
- [ ] `updateCompany` 액션을 API 호출 기반으로 변경
- [ ] `id` 타입 변경 영향 반영

### 4-4. 지원 상태 변경

- [ ] `PATCH /companies/{id}/status` 호출 함수 작성
- [ ] 요청 body를 `{ jobChangeStatus }`로 전송
- [ ] `updateApplicationStatus` 액션명 변경 또는 내부 구현 교체
- [ ] 칸반 이동 시 상태 변경 API가 정상 호출되는지 확인

### 4-5. 회사 삭제

- [ ] `DELETE /companies/{id}` 호출 함수 작성
- [ ] `deleteCompany` 액션을 API 호출 기반으로 변경
- [ ] 삭제 성공 후 목록 재조회 또는 로컬 상태 동기화

---

## 5. KanbanColumnConfig API 프론트 연동

### 5-1. 칸반 설정 조회

- [ ] `GET /kanban-columns` 호출 함수 작성
- [ ] 앱 초기화 시 칸반 설정을 API에서 불러오도록 변경
- [ ] API 응답이 없거나 실패할 경우 `DEFAULT_KANBAN_COLUMNS` fallback 유지
- [ ] Zustand `persist` 대상에서 `kanbanColumns` 제거

### 5-2. 칸반 설정 저장

- [ ] `PUT /kanban-columns` 호출 함수 작성
- [ ] 요청 body를 `{ columns }`로 전송
- [ ] `setKanbanColumns` 액션을 API 호출 기반으로 변경
- [ ] 칸반 컬럼 순서/표시 여부 변경 후 저장 동작 확인

---

## 6. Mock 데이터 정리

> 원칙: Mock 데이터는 외부 API 호출을 대체해야 하는 개발/테스트 상황에서만 사용한다. 앱의 일반 상태, 초기 데이터, 실제 화면 표시용 데이터에는 Mock을 사용하지 않는다.

- [ ] `useAppStore` 초기값에서 `MOCK_COMPANIES` 제거
- [ ] `useAppStore` 초기값에서 `MOCK_CRITERIA` 직접 사용 제거 또는 Criteria 로컬 정책에 맞게 정리
- [ ] `front/src/lib/mock.ts` 사용처 확인
- [ ] 외부 API 호출 대체 목적이 아닌 Mock 사용 제거
- [ ] 필요한 경우 외부 호출용 Mock만 API 계층 또는 테스트 코드로 범위 제한
- [ ] `front/src/lib/mock.ts` 삭제 또는 외부 호출 대체용으로만 역할 축소
- [ ] Mock 제거 후 빈 데이터 상태 UI가 자연스럽게 보이는지 확인

---

## 7. Criteria 처리

- [ ] `Criteria`를 백엔드 기능으로 추가할지 최종 결정
- [ ] 백엔드 확장 시 별도 Criteria API 설계
- [ ] 로컬 유지 시 `persist`에는 `criteriaList`만 남기도록 정리
- [ ] Criteria 관련 UI가 Company API 연동 후에도 깨지지 않는지 확인

---

## 8. 검증

- [ ] 백엔드 서버 실행 상태에서 프론트 연동 테스트
- [ ] 회사 목록 조회/생성/수정/상태 변경/삭제 플로우 확인
- [ ] 칸반 컬럼 조회/저장 플로우 확인
- [ ] 새로고침 후 서버 데이터 기준으로 상태가 복원되는지 확인
- [ ] 브라우저 콘솔 및 네트워크 탭에서 에러 확인
- [ ] 프론트 타입 체크 실행
- [ ] 프론트 린트 실행
- [ ] 프론트 빌드 실행
- [ ] 백엔드 테스트 실행

---

## 권장 작업 순서

1. 프론트 환경 설정
2. API 클라이언트 생성
3. 프론트 타입을 백엔드 기준으로 정리
4. 백엔드에 없는 Company 필드 처리 방향 결정
5. 회사 목록 조회 연동
6. 회사 생성 연동
7. 회사 수정 연동
8. 지원 상태 변경 연동
9. 회사 삭제 연동
10. 칸반 설정 조회 연동
11. 칸반 설정 저장 연동
12. Mock 데이터 정리
13. Criteria 처리 방향 확정
14. 전체 플로우 검증
