# Front ↔ Back API 연동 TODO

> 작성일: 2026-04-28  
> 코드 확인 기준: 2026-04-28

---

## 현재 구현 상태 요약

- [x] 백엔드 Company 기본 CRUD API 구현
  - [x] `GET /companies`
  - [x] `POST /companies`
  - [x] `PATCH /companies/{id}`
  - [x] `PATCH /companies/{id}/status`
  - [x] `DELETE /companies/{id}`
- [x] 백엔드 KanbanColumnConfig API 구현
  - [x] `GET /kanban-columns`
  - [x] `PUT /kanban-columns`
- [x] 백엔드 `Company.id`는 `Long` 기준으로 구현
- [x] 백엔드 지원 상태 필드명은 `jobChangeStatus` 기준으로 구현
- [x] 백엔드 `company`, `kanban_column_config` schema.sql 작성
- [x] 백엔드 Company 화면 확장 필드 저장/조회 구현
- [x] 프론트 API 클라이언트 구현
- [x] 프론트 `MOCK_COMPANIES`, `MOCK_CRITERIA` 직접 사용 제거
- [x] Zustand `persist` 대상에서 `companies`, `kanbanColumns` 제거

---

## 0. 남은 결정 사항

- [x] `Company.id`는 백엔드 기준 `Long`/프론트 `number`로 맞춘다
- [x] 지원 상태 필드명은 백엔드 기준 `jobChangeStatus`로 맞춘다
- [x] 백엔드에 없는 Company 필드는 Company API에 저장 필드로 확장한다
  - [x] `scores`
  - [x] `targetStatus`
  - [x] `jobPostingUrl`
  - [x] `recruitmentDeadline`
- [x] `Criteria` 기능은 프론트 로컬 상태로 유지한다

> 현재 백엔드 `Company`는 화면 복원에 필요한 `targetStatus`, `jobPostingUrl`, `recruitmentDeadline`, `scores`도 함께 저장한다. `Criteria` 설정 자체는 사용자별 로컬 설정 성격이 강하므로 현 단계에서는 `persist.criteriaList`에만 유지한다.

---

## 다음 작업 큐

1. [x] 프론트 dev 서버에서 `front/.env.local`의 `NEXT_PUBLIC_API_URL=http://localhost:8080/api` 변경사항 반영 확인
2. [x] 브라우저에서 실제 API 연동 플로우 테스트
3. [x] 테스트 중 발견되는 UI/API 오류 확인
4. [x] 백엔드에 없는 Company 필드 처리 방향 결정
5. [x] Criteria 기능을 백엔드로 올릴지 로컬 유지할지 결정
6. [ ] 백엔드 테스트 실행
7. [x] 테스트 결과에 맞춰 TODO 체크 업데이트

---

## 1. 프론트 환경 설정

- [x] `front/.env.local` 생성

```env
NEXT_PUBLIC_API_URL=http://localhost:8080/api
```

- [x] 프론트 빌드 시 `NEXT_PUBLIC_API_URL`이 정상적으로 읽히는지 확인
- [x] 프론트 dev 서버에서 브라우저 API 호출이 `http://localhost:8080/api` 기준으로 반영되는지 확인

> 백엔드 `server.servlet.context-path`가 `/api`로 잡혀 있으므로 base URL은 `http://localhost:8080/api`를 사용한다.

---

## 2. API 클라이언트 생성

- [x] `front/src/lib/api.ts` 생성
- [x] `NEXT_PUBLIC_API_URL` 기반 fetch wrapper 작성
- [x] 백엔드 공통 응답 형태 `ApiResponse<T>` 타입 정의
- [x] Spring Page 응답 타입 정의
- [x] 공통 HTTP 메서드 함수 작성
  - [x] `GET`
  - [x] `POST`
  - [x] `PATCH`
  - [x] `PUT`
  - [x] `DELETE`
- [x] 4xx, 5xx 응답 에러 핸들링 추가
- [x] 응답 JSON 파싱 및 빈 응답 처리 방식 정리

---

## 3. 프론트 타입 정리

- [x] `Company.id` 타입을 `string`에서 `number`로 변경
- [x] `Company.applicationStatus`를 `jobChangeStatus`로 변경
- [x] `ApplicationStatus` 타입명을 유지하고 백엔드 연동용 `JobChangeStatus` alias 추가
- [x] 백엔드 `CompanyResponse` 타입 추가
- [x] 백엔드 응답을 프론트 화면 모델로 변환하는 mapper 작성
- [x] 백엔드 확장 필드를 프론트 화면 모델로 매핑
  - [x] `scores`
  - [x] `targetStatus`
  - [x] `jobPostingUrl`
  - [x] `recruitmentDeadline`

---

## 4. Company API 프론트 연동

### 4-1. 회사 목록 조회

- [x] `GET /companies` 호출 함수 작성
- [x] 페이지 마운트 시 회사 목록 fetch
- [x] `useAppStore` 초기 `companies` 상태를 API 응답 기반으로 변경
- [x] Zustand `persist` 대상에서 `companies` 제거
- [x] 백엔드 Page 응답의 `content`를 화면 목록으로 매핑

### 4-2. 회사 생성

- [x] `POST /companies` 호출 함수 작성
- [x] 회사 생성 요청 DTO 타입 정의
  - [x] `name`
  - [x] `targetStatus`
  - [x] `jobPostingUrl`
  - [x] `recruitmentDeadline`
  - [x] `jobChangeStatus`
  - [x] `scores`
  - [x] `memo`
- [x] `addCompany` 액션을 API 호출 기반으로 변경
- [x] 생성 성공 후 회사 목록 재조회 또는 응답값으로 상태 동기화

### 4-3. 회사 수정

- [x] `PATCH /companies/{id}` 호출 함수 작성
- [x] 회사 수정 요청 DTO 타입 정의
  - [x] `name`
  - [x] `targetStatus`
  - [x] `jobPostingUrl`
  - [x] `recruitmentDeadline`
  - [x] `jobChangeStatus`
  - [x] `scores`
  - [x] `memo`
- [x] `updateCompany` 액션을 API 호출 기반으로 변경
- [x] `id` 타입 변경 영향 반영

### 4-4. 지원 상태 변경

- [x] `PATCH /companies/{id}/status` 호출 함수 작성
- [x] 요청 body를 `{ jobChangeStatus }`로 전송
- [x] `updateApplicationStatus` 액션 내부 구현 교체
- [x] 상세 모달에서 상태 변경 API가 정상 호출되는지 브라우저에서 확인
- [ ] 칸반 드래그 이동 시 상태 변경 API가 정상 호출되는지 브라우저에서 확인

### 4-5. 회사 삭제

- [x] `DELETE /companies/{id}` 호출 함수 작성
- [x] `deleteCompany` 액션을 API 호출 기반으로 변경
- [x] 삭제 성공 후 로컬 상태 동기화

---

## 5. KanbanColumnConfig API 프론트 연동

### 5-1. 칸반 설정 조회

- [x] `GET /kanban-columns` 호출 함수 작성
- [x] 앱 초기화 시 칸반 설정을 API에서 불러오도록 변경
- [x] API 응답이 없을 경우 `DEFAULT_KANBAN_COLUMNS` fallback 유지
- [x] Zustand `persist` 대상에서 `kanbanColumns` 제거

### 5-2. 칸반 설정 저장

- [x] `PUT /kanban-columns` 호출 함수 작성
- [x] 요청 body를 `{ columns }`로 전송
- [x] `setKanbanColumns` 액션을 API 호출 기반으로 변경
- [x] 칸반 컬럼 설정 저장 동작 브라우저에서 확인
- [ ] 칸반 컬럼 순서/표시 여부 변경 후 저장 동작 브라우저에서 확인

---

## 6. Mock 데이터 정리

> 원칙: Mock 데이터는 외부 API 호출을 대체해야 하는 개발/테스트 상황에서만 사용한다. 앱의 일반 상태, 초기 데이터, 실제 화면 표시용 데이터에는 Mock을 사용하지 않는다.

- [x] `useAppStore` 초기값에서 `MOCK_COMPANIES` 제거
- [x] `useAppStore` 초기값에서 `MOCK_CRITERIA` 직접 사용 제거
- [x] `front/src/lib/mock.ts` 사용처 확인
- [x] 외부 API 호출 대체 목적이 아닌 Mock 사용 제거
- [x] 필요한 경우 외부 호출용 Mock만 API 계층 또는 테스트 코드로 범위 제한
- [x] `front/src/lib/mock.ts` 삭제
- [x] Mock 제거 후 빈 데이터 상태 UI가 자연스럽게 보이는지 브라우저에서 확인

---

## 7. Criteria 처리

- [x] `Criteria`를 백엔드 기능으로 추가할지 최종 결정
- [x] Criteria는 현 단계에서 별도 백엔드 API를 만들지 않는다
- [x] 로컬 유지 시 `persist`에는 `criteriaList`만 남기도록 정리
- [x] Criteria 관련 UI가 Company API 연동 후에도 깨지지 않는지 확인

---

## 8. 검증

- [x] 백엔드 API base path 확인
  - [x] `http://localhost:8080/companies`는 404
  - [x] `http://localhost:8080/api/companies`는 200
- [x] 백엔드 서버 실행 상태에서 프론트 연동 테스트
- [x] 회사 목록 조회 확인
- [x] 회사 생성 플로우 확인
- [x] 회사 수정 플로우 확인
- [x] 회사 삭제 플로우 확인
- [ ] 칸반 드래그로 지원 상태 변경 확인
- [x] 상세 모달의 다음 단계 버튼으로 지원 상태 변경 확인
- [x] 칸반 컬럼 조회 확인
- [x] 칸반 컬럼 저장 확인
- [x] Mock 제거 후 빈 데이터 상태 UI 확인
- [x] Criteria 설정 UI 확인
- [x] 새로고침 후 서버 데이터 기준으로 상태가 복원되는지 확인
- [x] 브라우저 콘솔 에러 확인
- [x] 브라우저 네트워크 요청 URL이 `/api` 포함 경로인지 확인
- [x] 프론트 타입 체크 실행
- [x] 프론트 린트 실행
- [x] 프론트 빌드 실행
- [ ] 백엔드 테스트 실행

> 2026-04-28 검증 메모: `Codex API Test 20260428` 테스트 데이터를 생성 → 상태 변경(`APPLIED`) → 수정(`Codex API Test Updated`) → 삭제까지 확인했다. 삭제 후 `GET /api/companies` 응답은 빈 목록이었다. 브라우저 콘솔에는 런타임 에러는 없고 Radix Dialog의 `Description`/`aria-describedby` 누락 경고만 반복 출력됐다. 백엔드 테스트는 현재 로컬 `java -version`이 Java 8이라 Gradle/Spring Boot 3.5 요구사항(Java 17+)을 만족하지 못해 실행하지 못했다.

> 2026-04-28 추가 구현 메모: Company 확장 필드(`targetStatus`, `jobPostingUrl`, `recruitmentDeadline`, `scores`)를 백엔드 Entity/DTO/Response/schema에 추가하고 프론트 API 요청/응답 매핑에 연결했다. 프론트 `npx.cmd tsc --noEmit`, `npm.cmd run lint`, `node .\node_modules\next\dist\bin\next build`는 통과했다. 백엔드는 `JAVA_HOME=C:\Users\이준현\.jdks\corretto-21.0.9`로 `compileKotlin compileTestKotlin` 통과를 확인했다. 전체 `test` task는 Gradle Test Executor가 `worker.org.gradle.process.internal.worker.GradleWorkerMain`을 로드하지 못해 실패했다.

---

## 권장 작업 순서

1. 프론트 환경 설정
2. API 클라이언트 생성
3. 프론트 타입을 백엔드 기준으로 정리
4. 백엔드에 없는 Company 필드 처리 방향 결정
5. 회사 목록 조회 연동
6. 회사 생성 연동
7. 회사 수정 연동
8. 지원 상태 변경 연동
9. 회사 삭제 연동
10. 칸반 설정 조회 연동
11. 칸반 설정 저장 연동
12. Mock 데이터 정리
13. Criteria 처리 방향 확정
14. 전체 플로우 검증
