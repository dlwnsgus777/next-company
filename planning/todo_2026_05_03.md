# 회원/Google OAuth 후속 TODO

> 작성일: 2026-05-03  
> 기준 상태: Google OAuth 로그인 기본 연동 완료 후 남은 작업 정리

---

## 현재 완료된 범위

- [x] 백엔드 `spring-boot-starter-oauth2-client` 의존성 추가
- [x] Google OAuth2 로그인 엔드포인트 연결
  - [x] `GET /oauth2/authorization/google`
  - [x] `GET /login/oauth2/code/google`
- [x] Google OAuth 로그인 성공 시 우리 DB `member` 테이블에 회원 생성/갱신
- [x] `member` Entity/Repository 추가
- [x] Google `sub`, email, name, picture 저장
- [x] `GET /auth/me` 추가
- [x] `POST /auth/logout` 추가
- [x] 프론트 Google 로그인 화면 추가
- [x] 프론트 인증 세션 쿠키 전송 처리
- [x] 프론트 현재 로그인 사용자 상태 관리 추가
- [x] 프론트 로그아웃 버튼 추가
- [x] TODO 문서에 Google OAuth 기준 반영

---

## 남은 결정 사항

- [ ] 로그인 방식 범위 확정
  - [ ] Google OAuth만 사용할지 결정
  - [ ] 이메일/비밀번호 회원가입을 추가할지 결정
  - [ ] userna외할me 기반 로그인은 제지 결정
- [ ] 권한 모델 결정
  - [ ] 일반 사용자 `USER`만 둘지 결정
  - [ ] 관리자 `ADMIN` 권한이 필요한지 결정
- [ ] 비로그인 사용자 접근 정책 결정
  - [ ] 전체 차단
  - [ ] 읽기만 허용
  - [ ] 로그인 화면으로 이동
- [ ] 다른 사용자의 데이터 접근 시 응답 정책 결정
  - [ ] 존재하지 않는 것처럼 `404`
  - [ ] 권한 없음으로 `403`
- [ ] 기존 데이터 마이그레이션 정책 결정
  - [ ] 기존 회사/칸반 데이터를 특정 관리자 계정에 귀속
  - [ ] 기존 데이터를 초기화
  - [ ] 첫 로그인 사용자에게 임시 귀속

---

## 1. 백엔드 인증/보안 보강

- [ ] `SecurityConfig`의 `anyRequest().permitAll()` 정책 제거
- [ ] 인증 없이 접근 가능한 경로만 명시
  - [ ] `/oauth2/**`
  - [ ] `/login/oauth2/**`
  - [ ] `/auth/me`
  - [ ] `/auth/logout`
  - [ ] `/h2-console/**` 개발 환경 한정 허용 여부 결정
- [ ] Company API 인증 필요 처리
- [ ] KanbanColumnConfig API 인증 필요 처리
- [ ] 인증되지 않은 요청은 `401` 반환
- [ ] 권한 없는 요청은 `403` 또는 `404` 반환
- [ ] 인증 실패 응답 형식 `ApiResponse.fail(...)` 기준으로 정리
- [ ] CORS 설정에서 세션 쿠키 기반 인증 정책 재확인
  - [ ] `allowCredentials(true)` 유지
  - [ ] 프론트 origin 환경변수화 여부 검토
- [ ] 운영 환경에서 HTTPS/SameSite 쿠키 정책 검토

---

## 2. 회원 도메인 보강

- [ ] `MemberRole` enum 추가 여부 결정
  - [ ] `USER`
  - [ ] `ADMIN`
- [ ] `member` 테이블에 role 컬럼 추가 여부 결정
- [ ] Google OAuth 계정의 email 변경 가능성 처리 방식 검토
- [ ] provider/providerId unique 제약 테스트 추가
- [ ] email unique 제약과 provider 계정 연결 정책 검토
- [ ] 회원 탈퇴가 필요한지 결정
- [ ] 회원 탈퇴 시 회사/칸반 데이터 처리 정책 결정

---

## 3. 기존 데이터 회원별 분리

- [ ] `Company`에 owner/member 연관관계 추가
- [ ] `company` 테이블에 `member_id` FK 추가
- [ ] 회사 생성 시 현재 로그인 사용자를 owner로 저장
- [ ] 회사 목록 조회 시 현재 로그인 사용자의 회사만 반환
- [ ] 회사 단건 수정 시 owner 검증
- [ ] 회사 상태 변경 시 owner 검증
- [ ] 회사 삭제 시 owner 검증
- [ ] 다른 사용자의 회사 접근 차단
- [ ] `KanbanColumnConfig`에 owner/member 연관관계 추가
- [ ] `kanban_column_config` 테이블에 `member_id` FK 추가
- [ ] 칸반 설정 조회 시 현재 로그인 사용자의 설정만 반환
- [ ] 칸반 설정 저장 시 현재 로그인 사용자를 owner로 저장
- [ ] 사용자별 칸반 설정이 없을 때 기본 컬럼 반환
- [ ] 기존 `schema.sql`에 member FK 반영
- [ ] 기존 데이터 마이그레이션 SQL 작성 여부 결정

---

## 4. 백엔드 API 설계/구현

### 4.1 현재 사용자 조회

- [x] `GET /auth/me`
- [ ] 미로그인 상태 응답 형식 확정

예상 응답:

```json
{
  "success": true,
  "data": {
    "id": 1,
    "email": "user@example.com",
    "name": "홍길동",
    "pictureUrl": "https://example.com/avatar.png"
  },
  "message": null
}
```

### 4.2 로그아웃

- [x] `POST /auth/logout`
- [ ] 로그아웃 후 쿠키 삭제 동작 브라우저 검증
- [ ] 로그아웃 후 보호 API 접근 차단 검증

### 4.3 보호 API

- [ ] `GET /companies` 로그인 필요
- [ ] `POST /companies` 로그인 필요
- [ ] `PATCH /companies/{id}` 로그인 및 owner 검증 필요
- [ ] `PATCH /companies/{id}/status` 로그인 및 owner 검증 필요
- [ ] `DELETE /companies/{id}` 로그인 및 owner 검증 필요
- [ ] `GET /kanban-columns` 로그인 필요
- [ ] `PUT /kanban-columns` 로그인 필요

---

## 5. 프론트 인증 UX 보강

- [ ] 인증 확인 중 로딩 UI 개선
- [ ] 미로그인 상태에서 로그인 화면만 노출되는지 확인
- [ ] Google OAuth 실패 시 에러 화면 또는 메시지 추가
- [ ] `/auth/me` 401 응답 시 로그인 화면으로 전환
- [ ] 로그아웃 성공 후 민감 상태 초기화 확인
  - [ ] companies
  - [ ] kanbanColumns
  - [ ] 열린 모달
- [ ] Header의 사용자 표시 모바일 대응 확인
- [ ] 로그인 후 데이터 로딩 실패 메시지 표시 여부 검토
- [ ] `NEXT_PUBLIC_API_URL` 미설정 시 기본값을 `/api` 포함 형태로 맞출지 검토
- [ ] 브라우저 새로고침 후 로그인 상태 유지 확인

---

## 6. 테스트

### 6.1 백엔드 테스트

- [x] Google OAuth 회원 생성/갱신 서비스 테스트 작성
- [ ] Google OAuth 회원 생성/갱신 서비스 테스트 실행 환경 문제 해결
- [ ] `GET /auth/me` 로그인 상태 테스트
- [ ] `GET /auth/me` 미로그인 401 테스트
- [ ] `POST /auth/logout` 테스트
- [ ] Company API 미로그인 접근 차단 테스트
- [ ] Company API owner 검증 테스트
- [ ] KanbanColumnConfig API 미로그인 접근 차단 테스트
- [ ] KanbanColumnConfig API owner 검증 테스트
- [ ] 기존 Company/Kanban 서비스 테스트를 member 기준으로 수정

### 6.2 프론트 테스트/검증

- [x] 프론트 lint 통과 확인
- [x] 프론트 type check 통과 확인
- [x] 프론트 production build 통과 확인
- [ ] 브라우저에서 Google 로그인 플로우 검증
- [ ] 브라우저에서 로그아웃 플로우 검증
- [ ] 새로고침 후 인증 유지 검증
- [ ] 로그아웃 후 보호 API 접근 차단 검증
- [ ] 서로 다른 Google 계정 간 데이터 분리 검증

---

## 7. 환경 설정

- [ ] 로컬 실행 문서에 Google OAuth 환경변수 추가
  - [ ] `GOOGLE_CLIENT_ID`
  - [ ] `GOOGLE_CLIENT_SECRET`
  - [ ] `OAUTH2_SUCCESS_REDIRECT_URL`
- [ ] Google Cloud Console 설정 문서화
  - [ ] 승인된 JavaScript 원본: `http://localhost:3000`
  - [ ] 승인된 리디렉션 URI: `http://localhost:8080/api/login/oauth2/code/google`
- [ ] 운영 환경 리디렉션 URI 별도 정리
- [ ] secret 값이 git에 커밋되지 않는지 확인
- [ ] `.env`/`application-local.yml` 사용 방식 결정

---

## 권장 작업 순서

1. [ ] Google OAuth만 사용할지, 이메일/비밀번호 로그인을 추가할지 결정
2. [ ] Company/Kanban 데이터 owner 정책 결정
3. [ ] 백엔드 owner 연관관계 테스트 작성
4. [ ] `Company`에 member FK 연결
5. [ ] Company 조회/생성/수정/삭제 owner 기준으로 변경
6. [ ] `KanbanColumnConfig`에 member FK 연결
7. [ ] Kanban 조회/저장 owner 기준으로 변경
8. [ ] `SecurityConfig` 보호 경로 적용
9. [ ] 프론트 401 공통 처리 및 로그인 전환 보강
10. [ ] 브라우저에서 Google 로그인/로그아웃/새로고침/계정 분리 검증
11. [ ] 환경 설정 문서 업데이트
12. [ ] TODO 체크 업데이트

---

## 완료 기준

- [ ] Google 로그인 사용자가 `member` 테이블에 저장된다
- [ ] 미로그인 사용자는 보호 API를 사용할 수 없다
- [ ] 로그인 사용자는 자기 회사/칸반 데이터만 조회한다
- [ ] 다른 사용자의 회사/칸반 데이터 수정/삭제가 차단된다
- [ ] 로그아웃 후 보호 API 접근이 차단된다
- [ ] 새로고침 후 세션이 유지된다
- [ ] 서로 다른 Google 계정의 데이터가 섞이지 않는다
- [ ] 백엔드 테스트가 통과한다
- [ ] 프론트 lint/type check/build가 통과한다
- [ ] Google OAuth 로컬 실행 방법이 문서화되어 있다
